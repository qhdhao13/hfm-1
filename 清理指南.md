# 磁盘空间清理指南

## 当前状态
- 磁盘使用率：100%
- 剩余空间：约 135MB
- 需要清理：至少 5-10GB 空间

## 快速清理方法（按优先级排序）

### 方法一：使用系统自带的存储管理工具（最安全）

1. 点击苹果菜单 → **关于本机** → **存储空间**
2. 点击 **管理** 按钮
3. 系统会自动识别可清理的内容：
   - **优化存储**：自动删除已观看的电影和电视节目
   - **清空废纸篓**：一键清空
   - **减少混乱**：删除旧文件
   - **查看大文件**：手动删除不需要的大文件

### 方法二：手动清理命令（在终端执行）

#### 1. 清理废纸篓（立即释放空间）
```bash
rm -rf ~/.Trash/*
```

#### 2. 查找大文件（找出占用空间的文件）
```bash
# 查找下载文件夹中大于100MB的文件
find ~/Downloads -type f -size +100M -exec ls -lh {} \; | sort -k5 -hr | head -20

# 查找整个用户目录中大于1GB的文件
find ~ -type f -size +1G -exec ls -lh {} \; 2>/dev/null | sort -k5 -hr | head -20
```

#### 3. 清理系统缓存（安全）
```bash
# 清理用户缓存
rm -rf ~/Library/Caches/*

# 清理浏览器缓存（Chrome）
rm -rf ~/Library/Caches/Google/Chrome/*

# 清理浏览器缓存（Safari）
rm -rf ~/Library/Caches/com.apple.Safari/*
```

#### 4. 清理开发工具缓存
```bash
# Xcode缓存（如果安装了Xcode）
rm -rf ~/Library/Developer/Xcode/DerivedData/*
rm -rf ~/Library/Developer/Xcode/Archives/*

# npm缓存
npm cache clean --force

# pip缓存
pip cache purge
```

#### 5. 清理临时文件
```bash
# 清理下载文件夹中的临时文件
find ~/Downloads -name "*.tmp" -delete
find ~/Downloads -name "*.log" -delete
find ~/Downloads -name ".DS_Store" -delete
find ~/Downloads -name "*.cache" -delete

# 清理系统临时文件
sudo rm -rf /private/var/folders/*/*/C/*
```

#### 6. 清理Docker（如果安装了Docker）
```bash
docker system prune -a --volumes
```

#### 7. 清理Homebrew（如果使用Homebrew）
```bash
brew cleanup --prune=all
```

### 方法三：检查特定目录占用

```bash
# 查看各目录占用空间
du -sh ~/Downloads/* | sort -hr | head -20
du -sh ~/Library/* | sort -hr | head -20
du -sh ~/Documents/* | sort -hr | head -20
```

## 推荐清理顺序

1. **立即执行**：清空废纸篓
2. **检查大文件**：查找并删除不需要的大文件（视频、安装包等）
3. **清理缓存**：清理浏览器和系统缓存
4. **清理开发工具**：如果不需要，清理Xcode、npm等缓存
5. **使用系统工具**：使用"关于本机"中的存储管理工具

## 清理后检查

```bash
# 查看清理后的空间
df -h /
```

## 注意事项

⚠️ **重要提示**：
- 删除前请确认文件不再需要
- 建议先备份重要文件
- 系统缓存可以安全删除，但可能需要重新登录某些应用
- 如果使用Time Machine，确保备份是最新的

## 预防措施

1. 定期清理下载文件夹
2. 使用云存储存储大文件
3. 定期检查磁盘使用情况
4. 启用"优化存储"功能

