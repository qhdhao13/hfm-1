# 论语每日推送 - n8n工作流使用说明

## 📁 文件说明

- **lunyu-daily-workflow.json** - n8n工作流文件（可直接导入使用）

## 🚀 快速开始

### 第一步：导入工作流

1. 打开 n8n（本地或云端版本）
2. 点击左侧菜单的 **Workflows**（工作流）
3. 点击右上角的 **Import from File**（从文件导入）
4. 选择 `lunyu-daily-workflow.json` 文件
5. 导入成功后，工作流会显示在列表中

### 第二步：配置邮件发送

1. 点击工作流中的 **"发送邮件"** 节点
2. 点击 **Credentials**（凭据）下拉菜单
3. 选择 **Create New Credential**（创建新凭据）
4. 选择 **SMTP** 类型
5. 填写以下信息：

   **126邮箱SMTP配置：**
   - **Host（主机）**: `smtp.126.com`
   - **Port（端口）**: `465` (SSL) 或 `587` (TLS)
   - **User（用户名）**: `qhdhao@126.com`
   - **Password（密码）**: 你的邮箱授权码（不是登录密码！）
   - **Secure（安全连接）**: 选择 `SSL` 或 `TLS`

   **如何获取126邮箱授权码：**
   - 登录 126 邮箱网页版
   - 进入 **设置** → **POP3/SMTP/IMAP**
   - 开启 **SMTP服务**
   - 生成并复制 **授权码**（16位字符）
   - 将授权码填入密码字段

6. 点击 **Save**（保存）

### 第三步：配置微信推送

由于个人微信的限制，推荐使用以下方式之一：

#### 方式一：企业微信机器人（推荐）

1. 在企业微信群中添加机器人
2. 获取 Webhook URL（格式：`https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxxxx`）
3. 点击 **"发送到微信（企业微信Webhook）"** 节点
4. 将 URL 字段中的 `YOUR_WECHAT_WEBHOOK_URL` 替换为实际的 Webhook URL

#### 方式二：Server酱（简单易用）

1. 访问 https://sct.ftqq.com/ 注册账号
2. 获取 SendKey
3. 修改 **"发送到微信（企业微信Webhook）"** 节点：
   - **URL**: `https://sct.ftqq.com/YOUR_SENDKEY.send`
   - **jsonBody**: 改为 `={{ JSON.stringify({title: $json.chapterName, desp: $json.message}) }}`

#### 方式三：PushPlus

1. 访问 http://www.pushplus.plus/ 注册账号
2. 获取 Token
3. 修改 **"发送到微信（企业微信Webhook）"** 节点：
   - **URL**: `http://www.pushplus.plus/send`
   - **jsonBody**: 改为 `={{ JSON.stringify({token: 'YOUR_TOKEN', title: $json.chapterName, content: $json.message}) }}`

### 第四步：测试工作流

1. 点击右上角的 **Execute Workflow**（执行工作流）按钮
2. 查看每个节点的执行结果
3. 检查是否成功：
   - 获取到网页内容
   - 正确解析了篇章
   - 邮件发送成功
   - 微信推送成功

### 第五步：激活工作流

1. 点击工作流右上角的 **Active**（激活）开关
2. 工作流将每天自动运行

## ⚙️ 工作流说明

### 节点功能

1. **每天8点触发** - 定时触发器，每天上午8点自动执行
2. **获取论语网页** - 从 http://www.lunyu8.cn/ 获取完整网页内容
3. **解析并选择篇章** - 根据日期自动选择一篇论语（20篇循环）
4. **发送到微信** - 通过Webhook发送到微信
5. **发送邮件** - 发送到 qhdhao@126.com

### 篇章选择逻辑

- 使用年份中的天数（1-365）对20取模
- 每天自动选择不同的篇章
- 20篇论语循环推送

### 自定义设置

#### 修改触发时间

编辑 **"每天8点触发"** 节点，修改 cron 表达式：
- `0 8 * * *` - 每天8点
- `0 9 * * *` - 每天9点
- `0 20 * * *` - 每天20点
- `0 8 * * 1-5` - 工作日8点

#### 修改收件邮箱

编辑 **"发送邮件"** 节点的 `toEmail` 参数

#### 修改内容长度

编辑 **"解析并选择篇章"** 节点中的代码，修改 `substring(0, 2000)` 中的数字

## 🔧 故障排除

### 问题1：邮件发送失败

- **检查SMTP配置**：确认端口、加密方式正确
- **检查授权码**：确保使用的是授权码，不是登录密码
- **检查网络**：确保n8n可以访问 smtp.126.com

### 问题2：微信推送失败

- **检查Webhook URL**：确保URL正确且有效
- **检查网络**：确保n8n可以访问微信服务器
- **检查消息格式**：确保jsonBody格式正确

### 问题3：无法获取网页内容

- **检查网络**：确保n8n可以访问 http://www.lunyu8.cn/
- **检查网页结构**：如果网站改版，可能需要更新解析代码

### 问题4：篇章内容为空

- 可能是网页结构变化，需要更新正则表达式
- 检查 **"解析并选择篇章"** 节点的代码

## 📝 注意事项

1. **邮箱授权码**：必须使用授权码，不能使用登录密码
2. **微信限制**：个人微信无法直接接收，需要使用企业微信或第三方服务
3. **网络访问**：确保n8n运行环境可以访问外部网站
4. **时区设置**：确保n8n的时区设置正确，否则触发时间可能不对

## 🎯 工作流特点

- ✅ 自动按日期循环选择20篇论语
- ✅ 自动清理HTML标签，只保留文本内容
- ✅ 同时发送到微信和邮箱
- ✅ 每天自动执行，无需手动操作
- ✅ 内容长度自动限制，避免过长

## 📞 需要帮助？

如果遇到问题，可以：
1. 检查n8n的执行日志
2. 查看每个节点的输出数据
3. 根据错误信息调整配置

祝使用愉快！📚

