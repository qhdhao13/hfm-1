# n8n工作流运行说明

## ⚠️ 重要说明

**GitHub仓库本身不能直接运行n8n工作流**。n8n工作流文件（JSON格式）需要在n8n平台上运行。

## 🚀 运行方式

### 方式一：n8n云端（推荐，最简单）

1. **访问n8n云端**
   - 访问 https://n8n.io/
   - 注册免费账号（有免费额度）

2. **导入工作流**
   - 登录n8n云端
   - 点击 **Workflows** → **Import from File**
   - 上传 `lunyu-daily-workflow.json`
   - 配置邮件和微信推送（参考使用说明.md）

3. **激活工作流**
   - 点击右上角 **Active** 开关
   - 工作流将自动运行

**优点：**
- ✅ 无需自己搭建服务器
- ✅ 免费版有足够的使用额度
- ✅ 自动维护和更新
- ✅ 可以随时访问和管理

**缺点：**
- ⚠️ 免费版有执行次数限制
- ⚠️ 需要网络访问

### 方式二：自托管n8n（完全控制）

1. **安装n8n**
   ```bash
   # 使用Docker（推荐）
   docker run -it --rm \
     --name n8n \
     -p 5678:5678 \
     -v ~/.n8n:/home/node/.n8n \
     n8nio/n8n
   
   # 或使用npm
   npm install n8n -g
   n8n start
   ```

2. **访问n8n**
   - 打开浏览器访问 http://localhost:5678
   - 首次访问需要设置账号

3. **导入工作流**
   - 导入 `lunyu-daily-workflow.json`
   - 配置相关参数

**优点：**
- ✅ 完全免费，无限制
- ✅ 数据完全自己控制
- ✅ 可以自定义配置

**缺点：**
- ⚠️ 需要自己的服务器
- ⚠️ 需要自己维护
- ⚠️ 服务器需要24小时运行

### 方式三：使用GitHub Actions触发n8n（高级）

虽然GitHub Actions不能直接运行n8n工作流，但可以：

1. **通过Webhook触发n8n工作流**
   - 在n8n中创建工作流，使用Webhook触发器
   - 在GitHub Actions中调用n8n的Webhook URL

2. **示例GitHub Actions配置**

创建 `.github/workflows/trigger-n8n.yml`:

```yaml
name: 触发n8n工作流

on:
  schedule:
    # 每天UTC 0点（北京时间8点）执行
    - cron: '0 0 * * *'
  workflow_dispatch: # 允许手动触发

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: 调用n8n Webhook
        run: |
          curl -X POST ${{ secrets.N8N_WEBHOOK_URL }} \
            -H "Content-Type: application/json" \
            -d '{"trigger": "github-actions"}'
```

**注意：** 这需要：
- n8n工作流改为使用Webhook触发器（而不是Schedule触发器）
- 在GitHub Secrets中配置N8N_WEBHOOK_URL

## 📋 推荐方案

### 对于个人用户（推荐）
**使用n8n云端免费版**
- 简单易用
- 无需维护
- 免费额度足够日常使用

### 对于技术用户
**自托管n8n**
- 完全控制
- 无使用限制
- 可以部署在VPS或云服务器上

## 🔧 部署到云服务器示例

如果选择自托管，可以部署到：

1. **VPS服务器**（如阿里云、腾讯云、AWS等）
2. **Docker容器**（最简单）
3. **使用Docker Compose**（推荐）

**docker-compose.yml 示例：**

```yaml
version: '3.8'

services:
  n8n:
    image: n8nio/n8n
    container_name: n8n
    restart: always
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=your_password
    volumes:
      - ~/.n8n:/home/node/.n8n
```

## ❓ 常见问题

### Q: GitHub仓库能直接运行n8n工作流吗？
**A:** 不能。n8n工作流必须在n8n平台上运行。GitHub仓库只是存储配置文件的地方。

### Q: 可以用GitHub Actions代替n8n吗？
**A:** 可以，但需要重写工作流。GitHub Actions使用YAML格式，语法完全不同。如果要用GitHub Actions实现相同功能，需要重新编写工作流。

### Q: n8n云端免费版够用吗？
**A:** 对于每天执行一次的工作流，免费版通常足够使用。免费版一般有每月执行次数限制，但每天一次（约30次/月）通常没问题。

### Q: 如何选择运行方式？
**A:** 
- 新手或偶尔使用 → n8n云端
- 经常使用或需要更多控制 → 自托管
- 已有服务器 → 自托管

## 📝 下一步

1. 选择一种运行方式
2. 导入工作流文件
3. 配置邮件和微信推送
4. 测试并激活工作流

详细配置步骤请参考 `使用说明.md`

